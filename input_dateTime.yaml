# Radiateur Cuisine on

radiateur_cuisine_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur Cuisine off

radiateur_cuisine_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur sam on

radiateur_sam_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur sam off

radiateur_sam_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur entree on

radiateur_entree_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur entree off

radiateur_entree_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur ch jardin on

radiateur_ch_jardin_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur ch_jardin off

radiateur_ch_jardin_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur ch_rue on

radiateur_ch_rue_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur ch_rue off

radiateur_ch_rue_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur buanderie on

radiateur_buanderie_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur buanderie off

radiateur_buanderie_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur ch_parents on
radiateur_ch_parents_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur ch_parents off
radiateur_ch_parents_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur sdb on
radiateur_sdb_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur sdb off
radiateur_sdb_stop:
  name: Input with only time
  has_date: false
  has_time: true

####################################################################################

# Radiateur couloir_etage on
radiateur_couloir_etage_start:
  name: Input with only time
  has_date: false
  has_time: true

# Radiateur sdb off
radiateur_couloir_etage_stop:
  name: Input with only time
  has_date: false
  has_time: true
